name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --frozen

      - name: Build DocGen
        run: >
          uv run pyinstaller
          --name DocGen
          --windowed
          --onedir
          --icon "src/docgen/assets/docgen.ico"
          --add-data "src/docgen/assets:docgen/assets"
          --add-data "src/docgen/templates:docgen/templates"
          --collect-submodules docgen.documents
          src/docgen/main.py

      - name: Package release
        shell: pwsh
        run: |
          Copy-Item `
            src/docgen/examples/config.example.toml `
            dist/DocGen/config.toml

          Copy-Item `
            src/docgen/examples/logo.example.png `
            dist/DocGen/logo.png

          Compress-Archive `
            -Path dist/DocGen `
            -DestinationPath "DocGen-${{ github.ref_name }}-Windows.zip"

      - name: Create GitHub release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{ github.ref_name }}" `
            "DocGen-${{ github.ref_name }}-Windows.zip" `
            --verify-tag `
            --generate-notes `
            --title "DocGen ${{ github.ref_name }}"

  build-macos:
    needs: build-windows
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --frozen

      - name: Build DocGen
        run: >
          uv run pyinstaller
          --name DocGen
          --windowed
          --onedir
          --icon "src/docgen/assets/docgen.icns"
          --add-data "src/docgen/assets:docgen/assets"
          --add-data "src/docgen/templates:docgen/templates"
          --collect-submodules docgen.documents
          src/docgen/main.py

      - name: Package release
        run: |
          mkdir -p release/DocGen

          cp -R dist/DocGen.app release/DocGen/
          cp src/docgen/examples/config.example.toml release/DocGen/config.toml
          cp src/docgen/examples/logo.example.png release/DocGen/logo.png

          ditto -c -k \
            --sequesterRsrc \
            --keepParent \
            release/DocGen \
            "DocGen-${{ github.ref_name }}-macOS-arm64.zip"

      - name: Add macOS build to GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ github.ref_name }}" \
            "DocGen-${{ github.ref_name }}-macOS-arm64.zip"
